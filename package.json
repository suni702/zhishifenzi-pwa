{
  "name": "zhishifenzi-pwa",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "知食分子 PWA：本地优先的个人饮食决策与记忆助手。",
  "scripts": {
    "start": "node server.mjs",
    "dev": "PORT=4173 node server.mjs",
    "build:pages": "rm -rf public && mkdir -p public/assets && cp index.html styles.css app.js sw.js manifest.webmanifest public/ && cp assets/icon.svg public/assets/icon.svg",
    "check": "node --check server.mjs && node --check app.js && node --check sw.js && node --check functions/api/chat.js && node --check functions/api/health.js && node --check functions/api/transcribe.js"
  },
  "engines": {
    "node": ">=20"
  }
}
